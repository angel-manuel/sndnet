/**
 * @file
 * Contains common macros
 * */

 #ifndef SN_COMMON_H_
 #define SN_COMMON_H_

#define SN_MAX(a, b) ((a) > (b) ? (a) : (b))

#define SN_MIN(a, b) ((a) < (b) ? (a) : (b))

 #endif/*SN_COMMON_H_*/
